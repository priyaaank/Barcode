<%
  @messages.each do |message|
    user = message.user
%>
    $("#chat-area").append("<%= escape_javascript(render(:partial => 'message', :locals => {:sender => user.email, :text => message.text}))%>")
<%
  end
%>
